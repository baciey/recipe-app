import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 16,
  },
  activityIndicator: {
    marginTop: 50,
  },
  image: {
    height: 200,
    borderRadius: 12,
    marginBottom: 16,
  },
  name: {
    fontSize: 24,
    fontWeight: "bold",
    marginBottom: 8,
  },
  section: {
    marginTop: 16,
    fontWeight: "bold",
  },
});
